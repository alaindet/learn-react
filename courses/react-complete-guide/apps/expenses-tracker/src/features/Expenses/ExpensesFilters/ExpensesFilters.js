import './ExpensesFilters.css';

// TODO
export const ExpensesFilters = ({
  filters,
  onSelectFilters,
}) => {
  return (
    <div className="expenses-filters">
      ExpensesFilters
    </div>
  );
};
