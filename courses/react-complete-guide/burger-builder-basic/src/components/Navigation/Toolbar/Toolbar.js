import React from 'react';

import { ToolbarStyled, Navigation } from './Toolbar.style';

const Toolbar = (props) => {
  return (
    <ToolbarStyled>

      <div>MENU</div>

      <div>LOGO</div>

      <Navigation>
      </Navigation>

    </ToolbarStyled>
  );
};

export default Toolbar;
