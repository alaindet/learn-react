import styled from 'styled-components';

import theme from 'theme/theme';

const height = theme.height.toolbar + 16;

export const MainContent = styled.main`
  margin: 1rem;
  margin-top: ${height}px;
`;
